🎬 Movies App – Backend API

Live API:
👉 https://movies-app-backend-tau.vercel.app/

Base Endpoint:
👉 https://movies-app-backend-tau.vercel.app/api/movies/

📌 Overview

This is the backend REST API for the Movie Management Application.

It provides full CRUD functionality for managing movies using a structured MVC architecture.

The backend is built with Node.js, Express, and MongoDB (Mongoose) and deployed on Vercel.

🏗 Project Structure
movies-app-backend/
│
├── config/
│   └── db.js                # MongoDB connection
│
├── controllers/
│   └── movieController.js  # Business logic
│
├── models/
│   └── Movie.js            # Mongoose schema
│
├── routes/
│   └── movies.js           # API routes
│
├── .env                    # Environment variables
├── server.js               # App entry point
├── package.json

🛠 Tech Stack

Node.js

Express.js

MongoDB

Mongoose

CORS

dotenv

Vercel (Deployment)

⚙️ Setup Instructions (Local Development)
1️⃣ Clone Repository
git clone https://github.com/<your-username>/movies-app-backend.git
cd movies-app-backend

2️⃣ Install Dependencies
npm install

3️⃣ Create .env File

Create a .env file in root directory:

PORT=5000
MONGO_URI=your_mongodb_connection_string

4️⃣ Run Server
npm start


Server will run at:

http://localhost:5000

📡 API Endpoints
🔹 Get All Movies
GET /api/movies


Example:

https://movies-app-backend-tau.vercel.app/api/movies/

🔹 Get Single Movie
GET /api/movies/:id

🔹 Create Movie
POST /api/movies


Body (JSON):

{
  "name": "Inception",
  "genre": "Sci-Fi",
  "releaseYear": 2010,
  "rating": 9,
  "imageUrl": "https://image-url.jpg"
}

🔹 Update Movie
PUT /api/movies/:id

🔹 Delete Movie
DELETE /api/movies/:id

🧠 Architecture Pattern

The project follows MVC architecture:

Model → Mongoose Schema

Controller → Business logic

Routes → API routing

Config → Database connection

Server → Express initialization

🌍 Deployment

The backend is deployed on Vercel.

Live URL:

👉 https://movies-app-backend-tau.vercel.app/

🔒 Environment Variables

The following environment variables are required:

PORT
MONGO_URI


Do NOT commit .env to GitHub.